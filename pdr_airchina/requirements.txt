# Pinned to exact versions for reproducible builds (previously `>=` lower
# bounds only, so a fresh build could resolve a different, untested version
# at any time). Bump deliberately, re-running the test suite (backend/test_*)
# and the Docker build after each bump.
#
# This is the single source of truth for both the PyInstaller build
# (Dockerfile) and local dev — backend/requirements.txt was a second,
# looser-pinned copy of a subset of this file and has been removed.
fastapi==0.141.1
uvicorn[standard]==0.52.1
httpx==0.28.1
cryptography==50.0.0
